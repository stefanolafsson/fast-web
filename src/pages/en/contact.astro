---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Contact" description="Get in touch with FAST — the Association of Academic Staff at RU." pageKey="contact">
  <div class="page-header">
    <div class="container">
      <h1>Contact</h1>
      <p>Send us a line.</p>
    </div>
  </div>

  <div class="page-content">
    <div class="container">
      <div class="contact-grid">
        <div class="contact-info">
          <h2>Contact details</h2>

          <div class="contact-cards">
            <div class="contact-card">
              <div class="contact-icon">&#9993;</div>
              <div>
                <h3>Email</h3>
                <a href="mailto:fast@bhm.is">fast@bhm.is</a>
              </div>
            </div>

            <div class="contact-card">
              <div class="contact-icon">&#9962;</div>
              <div>
                <h3>Address</h3>
                <p>Reykjavík University<br />Menntavegur 1<br />102 Reykjavík</p>
              </div>
            </div>
          </div>

          <div class="bhm-note">
            <h3>BHM advisory services</h3>
            <p>
              FAST members can also turn to
              <a href="https://www.bhm.is" target="_blank" rel="noopener">BHM</a>
              with questions about terms and conditions, legal advice and other matters
              relating to their rights. BHM provides comprehensive advice to the
              members of its affiliated unions.
            </p>
          </div>
        </div>

        <div class="contact-form-section">
          <h2>Send a message</h2>
          <form class="contact-form" action="mailto:fast@bhm.is" method="get" enctype="text/plain">
            <div class="form-group">
              <label for="name">Name</label>
              <input type="text" id="name" name="name" required />
            </div>
            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" id="email" name="email" required />
            </div>
            <div class="form-group">
              <label for="subject">Subject</label>
              <input type="text" id="subject" name="subject" required />
            </div>
            <div class="form-group">
              <label for="body">Message</label>
              <textarea id="body" name="body" rows="5" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Send message</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-12);
  }

  .contact-info h2,
  .contact-form-section h2 {
    font-size: var(--size-2xl);
    margin-bottom: var(--space-8);
  }

  .contact-cards {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    margin-bottom: var(--space-10);
  }

  .contact-card {
    display: flex;
    align-items: flex-start;
    gap: var(--space-5);
    padding: var(--space-6);
    background: var(--color-bg-alt);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border-light);
  }

  .contact-icon {
    font-size: var(--size-2xl);
    line-height: 1;
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary);
    color: var(--color-text-inverse);
    border-radius: var(--radius-md);
  }

  .contact-card h3 {
    font-size: var(--size-base);
    color: var(--color-primary);
    margin-bottom: var(--space-1);
  }

  .contact-card p {
    color: var(--color-text-light);
    font-size: var(--size-sm);
    margin-bottom: 0;
    line-height: 1.6;
  }

  .contact-card a {
    font-size: var(--size-sm);
    color: var(--color-primary-light);
  }

  .bhm-note {
    background: var(--color-bg-alt);
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    border-left: 4px solid var(--color-primary-light);
  }

  .bhm-note h3 {
    font-size: var(--size-base);
    margin-bottom: var(--space-2);
  }

  .bhm-note p {
    color: var(--color-text-light);
    font-size: var(--size-sm);
    line-height: 1.7;
    margin-bottom: 0;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .form-group label {
    font-weight: 600;
    font-size: var(--size-sm);
    color: var(--color-text);
  }

  .form-group input,
  .form-group textarea {
    padding: var(--space-3) var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    font-family: var(--font-sans);
    font-size: var(--size-base);
    color: var(--color-text);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    background: var(--color-bg);
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary-light);
    box-shadow: 0 0 0 3px rgba(43, 108, 176, 0.1);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  @media (min-width: 1024px) {
    .contact-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
